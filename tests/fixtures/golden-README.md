# Golden List

Fixed dataset for golden tests

`redis` `cache`

> Ordered list · v2 · 2 items

## Setup

1. **Install Redis**
   Grab the binary

   ```sh
   apt install redis
   ```
   > why: speed

Intro **context**

2. **Configure** _(recommended)_

   ```sh
   redis-cli config set
   ```
