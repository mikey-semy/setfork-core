# Стиль базы — плотные строки (исторически код писался под ~110 колонок).
# use_small_heuristics = "Max": все эвристики ширины = max_width, меньше
# вертикального шума при переносах. Только stable-опции (fmt на stable).
max_width = 110
use_small_heuristics = "Max"
